const salaJS = ["Evaldo", "Camis", "Mari"];
const salaPython = ["Ju", "Leo", "Raquel"];

const salaJuntas = salaJS.concat(salaPython);

console.log(salaJuntas)

//Vale lembrar que a ordem em que informamos os arrays afeta a ordem em que os nomes aparecerão na lista final. 

//Vale reforçar mais uma vez que é importante nos atentarmos se o método retorna um novo array ou não.